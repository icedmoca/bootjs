# bootjs
UEFI boot environment with a fully programmable JavaScript interface, running on a minimal kernel, framebuffer compositor, and embedded QuickJS. It’s the first bootloader to use a full scripting engine in firmware, enabling a React-like, animated, themeable UI before any OS loads.
